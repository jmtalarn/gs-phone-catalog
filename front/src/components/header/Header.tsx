import React from 'react';

const Header = () => (
	<header data-testid="header" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
		<h1>Phone catalog</h1>
	</header>
);

export default Header;
